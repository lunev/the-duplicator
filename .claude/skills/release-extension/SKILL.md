---
name: release-extension
description: Build and package a new release of The Duplicator Chrome extension — bumps app/package.json's version to match app/public/manifest.json, records the release in README.md's Changelog section and the in-extension "What's New" banner, runs the production build, and confirms the release zip was created. Only invoke when the user explicitly asks to release, build a release, or cut a new version.
disable-model-invocation: true
---

Release flow for The Duplicator (Chrome MV3 extension):

1. Read the `version` field in `app/public/manifest.json` — this is the source of truth for the release version. If the user asked to bump the version (e.g. "release 7.0.5"), update `app/public/manifest.json`'s `version` first.
2. Update `app/package.json`'s `version` field to match `app/public/manifest.json`'s `version` exactly. These two fields are maintained independently in this repo and must be kept in sync manually.
3. Figure out what changed: find the previous release commit with `git log --oneline -- app/public/manifest.json` (each release is a version-bump commit), then review the commits/diff since then.
4. Add a new entry under README.md's `## Changelog` section for this version, covering **only user-visible changes**: new features, redesigned UI, changed behavior/interactions a user would notice. Leave out anything with no visible effect — dependency/framework upgrades, refactors, internal validation logic, build tooling, tests, lint, CI, dev-only scripts, etc. This section is the permanent historical record across all releases — always append a new `### <version> - <date>` entry above the previous ones, never overwrite past entries. If a release has no user-visible changes, add an entry saying so (e.g. "Internal changes only") rather than skipping it or padding it with internal work.
5. Sync `app/src/constants/changelog.ts`'s `CHANGELOG` constant to match the new README.md entry — set `version` to the new version and `features` (a `{title, description}[]`; short title, one plain-language sentence each) to the same user-visible items just added to the changelog. This constant only ever holds the *latest* release's notes — it's what `UpdateInfo.tsx` shows as a dismissible "What's New" banner after a user's extension auto-updates (flag set by `chrome.runtime.onInstalled` in `service-worker.ts`). If the changelog entry was "no user-visible changes," leave `CHANGELOG` as it was from the last release that had real changes, so the banner never shows an empty/pointless update.
6. Run `npm run build` from inside `app/`. This runs `tsc -b` (typecheck), a production `vite build`, then `build-zip.js`, which zips the `app/build/` output into `chrome-webstore/releases/<name>-v<version>.zip` using the version from `app/public/manifest.json`.
7. Confirm the new zip exists at `chrome-webstore/releases/the-duplicator-v<version>.zip` (or the equivalent sanitized name `build-zip.js` produces) and report the path back to the user.
8. The resulting zip (plus the version, `README.md`, and `app/src/constants/changelog.ts` changes) is meant to be committed to git (release zips under `chrome-webstore/releases/` are tracked in this repo) — stage and mention it, but don't commit or push without the user's explicit go-ahead.
