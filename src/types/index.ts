export type Preferences = {
  basicMode: boolean;
  newTab: boolean;
  sidePanel: boolean;
};

export type Param = {
  id: string;
  title: string;
};

export type Icon = {
  size?: string;
  color?: string;
};
